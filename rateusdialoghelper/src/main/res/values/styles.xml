<resources>

    <style name="TextSmallMedium">
        <item name="android:textSize">@dimen/text_size_small_medium</item>
    </style>

    <style name="myRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/user_rating_stars</item>
        <item name="android:indeterminateDrawable">@drawable/user_rating_stars</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:maxHeight">30dp</item>
    </style>

</resources>
